//- template.pug

doctype html
html
  head
    link(href='/static/style.css' type='text/css' rel='stylesheet')
  body
    h1 #{title}
    ul.linkWrapper
      each val in pngFileList
        li(class='linkRow')
          p #{val.title}
          img(class='imgBlock' src=val.imgUrl)
          